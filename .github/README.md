# CI and publication

**Tests** runs the full cross-browser/platform regression suite. **GitHub Pages**
deploys only a successfully tested current `main` commit.
See [CI organization, coverage and local commands](../docs/ci.md).
